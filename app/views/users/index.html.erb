<% @users.each do |user| %>
  <div>
      <img src=<%= user.photo %> alt="user-profile" />
      <div>
        <h2><%= user.name %></h2>
        <p>Number of posts: <%= user.posts_counter %></p>
      </div>
  </div>
<% end %>
